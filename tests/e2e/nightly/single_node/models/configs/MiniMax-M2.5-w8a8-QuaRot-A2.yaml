# ==========================================
# ACTUAL TEST CASES
# ==========================================

test_cases:
  - name: "MiniMax-M2.5-w8a8"
    model: "Eco-Tech/MiniMax-M2.5-w8a8-QuaRot"
    envs:
      HCCL_BUFFSIZE: "1200"
      HCCL_OP_EXPANSION_MODE: "AIV"
      PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
      VLLM_ASCEND_ENABLE_FLASHCOMM1: "1"
      OMP_NUM_THREADS: "1"
      TASK_QUEUE_ENABLE: "1"
      VLLM_ASCEND_ENABLE_FUSED_MC2: "1"
      VLLM_ASCEND_ENABLE_NZ: "1"
      VLLM_TORCH_PROFILER_WITH_STACK: "0"
      VLLM_TORCH_PROFILER_DIR: "./profile"
      VLLM_USE_MODELSCOPE: "true"
      SERVER_PORT: "DEFAULT_PORT"
    server_cmd:
      - "--tensor-parallel-size"
      - "8"
      - "--port"
      - "$SERVER_PORT"
      - "--trust-remote-code"
      - "--gpu-memory-utilization"
      - "0.9"
      - "--quantization"
      - "ascend"
      - "--speculative_config"
      - '{"method":"eagle3","model":"vllm-ascend/MiniMax-M2.5-eagle-model-0318","num_speculative_tokens":3}'
      - "--enable-expert-parallel"
      - "--enable-chunked-prefill"
      - "--no-enable-prefix-caching"
      - "--max-num-seqs"
      - "16"
      - "--max-model-len"
      - "131072"
      - "--seed"
      - "1024"
      - "--max-num-batched-tokens"
      - "16384"
      - "--enable-auto-tool-choice"
      - "--tool-call-parser"
      - "minimax_m2"
      - "--reasoning-parser"
      - "minimax_m2_append_think"
      - "--enable-force-include-usage"
      - "--profiler-config"
      - '{"profiler":"torch","torch_profiler_dir":"./profile","torch_profiler_with_stack":false}'
      - "--compilation-config"
      - '{"cudagraph_mode": "FULL_DECODE_ONLY"}'
      - "--additional-config"
      - '{"enable_cpu_binding": true, "enable_npugraph_ex": true, "enable_static_kernel": true}'
    benchmarks:
      acc:
        case_type: accuracy
        dataset_path: vllm-ascend/gpqa
        request_conf: vllm_api_general_chat
        dataset_conf: gsm8k/gpqa_gen_0_shot_str
        # Temporary PR smoke configuration: one bounded request is enough to
        # validate readiness, the first ACLGraph replay, and benchmark exit.
        max_out_len: 1024
        batch_size: 1
        num_prompts: 1
        baseline: 50
        threshold: 50
        bos_token_id: 200019
        do_sample: true
        eos_token_id: 200020
        temperature: 1.0
        top_p: 0.95
        top_k: 40
        transformers_version: 4.46.1
        ignore_eos: false
      # Keep the PR validation focused on the startup/first-request path.
      # Restore the full accuracy and performance settings after validation.
      perf: {}
