name: Test Liqo 910b

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: linux-aarch64-910b-1
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/modelfoundry/ascend-ci-base:latest
    steps:
      - run: npu-smi info && sleep 60
