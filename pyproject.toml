[build-system]
# Should be mirrored in requirements.txt
requires = [
    "setuptools>=64",
    "setuptools-scm>=8",
    "cmake"
    "decorator",
    "pyyaml",
    "scipy",
]
build-backend = "setuptools.build_meta"
