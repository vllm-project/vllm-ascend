[build-system]
# Should be mirrored in requirements.txt
requires = [
    "setuptools>=64",
    "setuptools-scm>=8",
    "cmake>=3.26",
    "decorator",
    "pyyaml",
    "scipy",
]
build-backend = "setuptools.build_meta"
