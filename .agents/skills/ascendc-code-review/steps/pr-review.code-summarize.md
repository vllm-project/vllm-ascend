# 代码概要 + 侧别识别（PR 检视）

## 派发

派发子 Agent 执行代码概要生成。`subagent_type` 按优先级选择：

1. `"ascendc-code-summarizer"` — 首选
2. `"general"` — 兜底，传入相同 prompt

```
Agent({
  subagent_type: "ascendc-code-summarizer 或 general",
  description: "代码概要：梳理 PR 代码脉络",
  prompt: "代码概要生成（PR 检视模式）

【输入】
- diff 文件路径：{diff_file_path}
- 完整源码路径：{repo_path}
- 概要输出路径：{code_summary_output_path}

【执行要求】
1. 严格按本文件「子 Agent 执行指南」中定义的 8 步流程执行（PR 模式）
2. 概要写入输出路径，禁止跳过侧别识别
3. 返回结构化结果（含侧别识别，供后续阶段使用）

【执行指南位置】
Read 本文件（steps/pr-review.code-summarize.md）的「子 Agent 执行指南」章节获取完整指令。
"
})
```

---

## 子 Agent 执行指南

以下内容为子 Agent 的执行指令，主 Agent 不执行。

### 核心原则

1. **理解优先** — 先读懂代码整体设计，再按模板填空，禁止机械填表
2. **证据驱动** — 每个结论必须有 Read/Grep 证据支撑，禁止推测
3. **侧别必识别** — Kernel/Tiling 侧别判定是后续条例过滤的关键输入

---

## 关键知识

### Tiling/Kernel 分层

| 侧别 | 文件位置 | 代码特征 | 职责 |
|------|---------|---------|------|
| **Kernel 侧** | `op_kernel/*.cpp/.h` | `__aicore__`、`AscendC::`、`pipe.InitBuffer` | 在 AI Core 上执行计算，不做参数校验 |
| **Tiling 侧** | `op_host/*.cpp/.h` | `gert::TilingContext`、`TilingData` | 参数校验、资源计算、多核切分 |

### 变量来源判定

| 来源 | 特征 | 校验状态 |
|------|------|----------|
| Tiling 传递 | `tilingData.GetXXX()` | 已校验，无需重复 |
| 硬件配置 | `GetCoreNumAic()`、`GetCoreNumAiv()` 等平台 API | 硬件保证，构造阶段已校验 |
| 常量定义 | `constexpr` / `const` | 编译期固定 |
| 架构常量 | `FP32_*` / `UB_*` / `BLOCK_*` | 硬件固定 |

### 代码关联追踪

| 追踪方向 | 关键线索 | 方法 |
|----------|----------|------|
| **上游** | `#include` 语句 | 从 include 定位 TilingData 结构定义 |
| **上游** | 函数调用者 | 从入口函数名 Grep 反推调用位置 |
| **下游** | 函数调用 | 从 Compute/Process 等函数看调用链 |
| **下游** | AscendC API | DataCopy/Reduce 等 API 定位计算模块 |

---

## 执行流程

```
Step 1: 读取代码     — 先 Read diff，再 Read 变更文件完整源码
Step 2: 识别侧别     — 按 Tiling/Kernel 分层特征判定侧别
Step 3: 梳理脉络     — 每个变更文件独立成章，汇总跨文件关联
Step 3.5: 业务语义   — Kernel侧: 数学运算+计算模式+同步契约; Tiling侧: 切分策略+校验语义+Buffer规划
Step 4: 追踪关联     — Read include 头文件 + Grep 跨文件调用链
Step 5: 变量溯源     — 对 diff 中出现的类成员/全局变量 grep 声明→初始化→校验链
Step 6: 分析设计     — Kernel侧：流水线模式/切分/Buffer管理（需量化）
Step 7: 生成概要     — 按 PR 模板填充，写入输出路径
Step 8: 返回结果     — 结构化返回侧别和脉络摘要
```

### Step 1: 读取代码

1. 先 Read diff 文件，提取变更文件列表和变更行范围
2. 对每个变更文件，Read 完整源码（路径 = `{repo_path}/{file_path}`）
3. 结合 diff 和完整源码：diff 告诉你改了什么，完整源码告诉你变量从哪来、上游做了什么校验

### Step 2: 识别侧别

按关键知识中的侧别特征表判定。如有 TilingData 结构定义文件（`*_tiling_data.h`），也必须 Read，确认字段名与代码中一致。PR 场景需对每个变更文件识别侧别，输出「Kernel/Tiling/混合」。

### Step 3: 梳理脉络

对每个变更文件分别梳理，先概览所有变更文件（文件+侧别+变更类型），再逐文件分析脉络和变量溯源，最后汇总跨文件关联。

| 脉络节点 | 分析内容 | 验证方法 |
|----------|----------|----------|
| **入口** | 哪个函数是入口？被谁调用？触发条件是什么？ | Read + Grep 搜索函数名 |
| **数据流** | 数据从哪来（GM/Tiling）→ 经哪处理 → 输出到哪 | Read include + TilingData 结构 |
| **计算核心** | 主循环在哪？核心 API 是什么？关键变量如何流转 | Read 主循环代码，定位循环边界和 API 调用 |
| **输出** | 结果写回哪里？同步机制是什么？ | Read 写回代码，确认 EnQue/DeQue |

### Step 3.5: 业务语义分析

基于 Step 3 已梳理的代码脉络，按侧别执行对应的业务语义分析。PR 模式下对每个变更文件分别分析，标注业务语义是否由本次变更引入。

#### 3.5K: Kernel 侧业务语义（仅 Kernel/混合侧执行）

**a. 算子数学运算推断**
- 从函数名、变量名、API 调用模式推断数学公式
- 识别输入输出拓扑（几输入几输出、各 tensor 语义角色）
- 识别数学不变量

**b. 计算模式识别**（7 种模式，按优先级逐条 Grep，命中即停）

| # | 模式 | 识别信号 |
|---|------|---------|
| 1 | Simple Vector Pipeline | 默认 |
| 2 | Double Buffer Vector Pipeline | `BUFFER_NUM = 2` |
| 3 | Multi-Step Vector Decomposition | `TBuf<VECCALC>` + chained API |
| 4 | Compile-Time Branch Dispatch | `if constexpr` |
| 5 | AIC-AIV MIX Cooperative | `ASCEND_IS_AIC` + `CrossCoreSetFlag` |
| 6 | 5-Stage Cube Pipeline | `block_mmad` + `CopyGM2L1` + `Fixpipe` |
| 7 | DAG Declarative (atvoss) | `DAGSch` + `Bind` + `Placeholder` |

**c. 分支业务含义** — 对每个分支补充业务解释（尾部处理/精度路径/模板分发等）

**d. 模板参数语义** — 标注每个模板参数的业务含义

**e. 同步契约分层** — 标注同步机制所属层次（MTE↔Vector / AIC↔AIV / 跨核）

#### 3.5T: Tiling 侧业务语义（仅 Tiling/混合侧执行）

**a. 校验策略分析** — 对每个 OP_CHECK_IF 标注校验的数学不变量

**b. 多核切分策略分析** — 识别切分类型（按元素/按网格/按reduce轴），标注切分变量业务含义

**c. Buffer 规划策略分析** — 单/双缓冲决策依据、UB 分配公式

**d. TilingKey 语义分析** — 每个轴的业务含义

**e. Workspace 数学来源** — 临时空间的数学来源

### Step 4: 追踪关联 + 跨文件防御分析

- **必须** Read `#include` 的头文件，追踪 TilingData 结构定义、基类成员声明
- Grep 搜索入口函数名和关键函数名，追踪跨文件调用链
- 识别代码中使用的 AscendC API 清单
- 对 diff 中出现的类成员变量（如 `aicNum_`），grep 完整源码找到声明和初始化位置
- **关键**：PR 模式拥有完整源码仓（`{repo_path}`），必须利用此优势做深度跨文件防御分析：
  - 对 diff 中出现的每个关键变量，跨文件 grep 追溯其完整的声明→初始化→校验链
  - 特别关注未变更文件中的校验代码——这些是子 Agent 检视时判定"上游已校验"的关键证据
  - **将跨文件发现的校验代码、初始化、常量定义写入输出的「跨文件防御摘要」表**
- **必须追踪 TilingData 字段的 Host 侧来源**（PR 模式独有优势——完整源码仓含 op_host/）：
  - 从 Kernel 变更代码中提取所有 `tilingData->xxx` / `tilingData_.xxx` 字段读取
  - 在完整源码仓中 Grep 每个字段在 op_host/*.cpp 中的赋值语句
  - Read 赋值上下文，提取计算该字段的完整公式、输入参数和约束条件
  - 将结果写入「TilingData 值域溯源」表
- **必须提取芯片架构参数**：从 platformInfo_ 中提取核数、对齐常量等，写入「芯片架构参数」表。若无法从本地文件确定具体数值，使用 `/npu-arch` skill 查询对应芯片代际的硬件参数

### Step 5: 变量溯源

对 diff 中使用的类成员变量和全局变量，在完整源码中 grep 追溯：
- 声明位置（类定义中的成员声明，含类型和默认值）
- 初始化位置（构造函数 / Init / SetXXX 函数中的赋值语句）
- 校验位置（`OP_CHECK_IF`、`assert`、条件判断等防护代码）
- 来源类型（TilingData传递 / 硬件配置 / 编译期常量 / 外部输入）

**校验代码的具体行号和内容必须写入输出的「跨文件防御摘要」表和「变量溯源」表。** 无法确认的标注「未在源码中找到」。

### Step 5.1: 生成函数清单

Grep 变更文件中的函数定义，填入输出「函数清单」表：函数名、签名、行范围、角色、是否本次变更。

### Step 5.2: 生成 API 调用索引

Grep 变更文件中的所有 AscendC API 调用，填入输出「API 调用索引」表：API 名、行号、上下文、是否本次新增。

### Step 5.3: 生成常量清单

Grep 变更文件中的编译期常量，填入输出「常量清单」表：常量名、值、行号、用途、是否本次变更。

### Step 6: 分析高性能设计（仅 Kernel 侧）

- 识别流水线模式（同步/异步/AIC-AIV 协同/纯 Vector）
- 检查 EnQue/DeQue 同步机制
- 识别 Buffer 管理模式（单缓冲 / Double Buffer / N-Buffer），标注 buffer 名称和大小(B)
- 分析多核切分策略（按哪个维度切分，每核处理量，需给出具体数值）
- 分析 UB 切分策略（单次处理量，是否分 chunk，给出切分粒度）

### Step 7: 生成概要

按 PR 输出模板填充，写入输出路径。**确保所有字段都基于代码证据填充，无法确定的字段标注原因。**

### Step 8: 返回结果

向主流程返回结构化结果（不写报告文件，概要已写入输出路径）。

---

## 输出模板（PR 模式）

```markdown
# PR 代码概要

PR: #{number} | 算子: {name} | 侧别: {Kernel/Tiling/混合}

## 变更文件概览

| # | 文件 | 侧别 | 变更类型 | 说明 |
|---|------|------|---------|------|
| 1 | {path} | {Tiling/Kernel} | {新增/修改/删除} | {一句话说明} |

## 文件1: {path}（{Tiling/Kernel}侧）

### 变更概要
{该文件改了什么、为什么改}

### 代码脉络
**入口**: {函数名} → **数据流**: {描述} → **计算核心**: {描述} → **输出**: {描述}

**分支覆盖**:
| 分支条件 | 位置(行) | 触发场景 | 处理逻辑 | 涉及 API |
|---------|---------|---------|---------|----------|

**关键变量**:
| 变量 | 来源 | 用途 |
|------|------|------|

### 业务语义（Kernel 侧，若本文件为 Kernel 侧）

**数学运算**: {公式} | **计算模式**: {模式} | **同步契约**: {各层同步}

**分支业务含义**:
| 分支条件 | 位置(行) | 业务含义 | 变更? |
|---------|---------|---------|-------|

**模板参数语义**:
| 参数 | 取值 | 业务含义 | 变更? |
|------|------|---------|-------|

### 业务语义（Tiling 侧，若本文件为 Tiling 侧）

**切分策略**: {类型} | **Buffer策略**: {策略}

**校验策略**:
| 校验条件 | 位置(行) | 数学不变量 | 变更? |
|---------|---------|-----------|-------|

**切分变量语义**:
| 变量 | 公式 | 业务含义 | 变更? |
|------|------|---------|-------|

**TilingKey 语义**:
| 轴 | 取值 | 业务含义 | 变更? |
|----|------|---------|-------|

### 变量溯源
| 变量 | 声明(文件:行) | 初始化(文件:行) | 校验(文件:行) | 来源类型 |
|------|-------------|----------------|-------------|---------|

## 文件2: {path}（{Tiling/Kernel}侧）
...

## 函数清单（全变更文件汇总）

| 函数 | 签名 | 行范围 | 所属文件 | 角色 | 变更? |
|------|------|--------|---------|------|-------|
| {func1} | {完整签名} | {start}-{end} | {file} | 入口/计算/辅助 | 新增/修改 |

## API 调用索引（全变更文件汇总）

| API | 行号 | 所属文件 | 上下文 | 变更? |
|-----|------|---------|--------|-------|
| DataCopy | 234 | split_m.h | tokenX→UB | 新增 |

## 常量清单（全变更文件汇总）

| 常量 | 值 | 位置(文件:行) | 用途 | 变更? |
|------|-----|-------------|------|-------|
| FINISH_VEC_DEQUANT | 0X7 | comm.h:30 | 同步标志 | 修改 |

## TilingData 值域溯源（全仓）

> PR 模式独有优势——完整源码仓含 op_host/。每个 Kernel 侧 TilingData 字段追溯其在 Host 侧的计算公式和输入来源。

| TilingData 字段 | Host 侧计算(文件:行) | 公式 | 输入参数 | 约束 | 变更? |
|---------------|---------------------|------|---------|------|-------|
| {field} | {op_host/xxx.cpp:行} | {计算公式} | {shape/coreNum/flag} | {边界条件} | {新增/修改/已有} |

## 芯片架构参数

| 参数 | 值 | 来源 | 影响范围 |
|------|-----|------|---------|
| aivNum | {值} | platformInfo_.aivNum | 单核分配量、偏移上限 |

## 跨文件防御摘要

> 以下发现来自完整源码仓的跨文件追踪（Step 4），供后续通用检视子 agent 直接利用，避免重复 grep。PR 模式下的关键优势——能访问未变更文件中的上游校验代码。

| 关联文件 | 关键发现 | 位置(文件:行) | 变更文件? | 影响范围 |
|---------|---------|-------------|----------|---------|
| {file1} | {发现的校验代码、初始化、常量定义} | {文件:行号} | 是/否 | {影响哪些变量/操作} |
| {file2} | {发现的校验代码、初始化、常量定义} | {文件:行号} | 是/否 | {影响哪些变量/操作} |

## 跨文件关联

### Tiling → Kernel 数据流
{TilingData 结构名} 中本次涉及的字段（Tiling 填充 → Kernel 使用）:
| TilingData 字段 | Tiling 填充位置 | Kernel 使用位置 | 用途 |

### 同步标志 / 常量关联
| 定义位置 | 值 | 引用位置 | 影响范围 |

### 模板派发链（如有）
| 条件 | 派发目标 | 所在文件 |

### Include / 调用链
{关键跨文件 include 和函数调用关系}
```

---

## 返回格式

```
侧别: {Kernel侧 / Tiling侧 / 混合}
算子名: {operator_name}
功能概述: {一句话功能描述}
入口函数: {函数名}
核心 API: {API 列表}
概要路径: {输出路径}
```

---

## 约束

- 代码概要**必须**写入输出路径（后续检视步骤将此文件作为输入）
- **禁止**跳过 Step 2 侧别识别，侧别判定必须基于代码证据
- **禁止**凭推测填写模板字段，无法确定的标注具体原因
- **必须**用 Grep 追踪上游调用链和 TilingData 结构定义
- **必须**在返回结果中明确标注侧别
