# 环境兼容性表

基于 `asc-devkit` 仓库的版本和环境要求信息。

---

## 当前项目环境

| 项目 | 值 |
|-----|-----|
| 服务器类型 | A3 服务器 |
| CANN 版本 | 8.5.0 |
| 工作目录 | 项目根目录 |

---

## asc-devkit 版本信息

| 项目 | 值 |
|-----|-----|
| 当前版本 | **9.0.0** |
| 版本目录 | cann |
| 代码位置 | `$ASC_DEVKIT_DIR/`（项目根目录下） |

---

## 依赖版本要求

| 组件 | 要求版本 |
|-----|---------|
| runtime | 9.0.0 |
| metadef | 9.0.0 |
| opbase | 9.0.0 |
| bisheng-compiler | 9.0.0 |
| ge-executor | 9.0.0 |
| tbe-tik | 9.0.0 |

---

## NPU 架构兼容性

| NPU 架构 | 说明 |
|---------|------|
| `dav_m200` | NPU ARCH 200x 架构 |
| `dav_c220` | NPU ARCH 220x 架构 |
| `npu_arch_2201` | NPU ARCH 220X 架构 |
| `npu_arch_3510` | NPU ARCH 351X 架构 |

---

## 开发环境要求

| 工具 | 最低版本 | 推荐版本 |
|-----|---------|---------|
| Python | 3.9.0 | - |
| GCC | 7.3.0 | - |
| CMake | 3.16.0 | - |
| Patch | 必需 | - |
| lcov | 1.16（可选，UT 用） | - |
| pytest | 8.0.0（可选，UT 用） | - |
| coverage | 4.5.4（可选，UT 用） | - |
| googletest | release-1.14.0（可选） | - |

---

## 版本兼容性注意事项

### 1. CANN 版本差异

**当前情况**：
- 项目环境：CANN 8.5.0
- asc-devkit：版本 9.0.0

**影响**：
- 部分新 API 可能需要 CANN 9.0.0 支持
- 建议查阅 API 文档确认版本要求
- 使用新 API 前先验证当前环境兼容性

### 2. 数据类型支持

| 类型 | 说明 |
|-----|------|
| `float` | 32 位浮点 |
| `float16` / `half` | 16 位浮点 |
| `bfloat16` | BFloat16 格式 |
| `int8` / `uint8` | 8 位整数 |
| `int16` / `uint16` | 16 位整数 |
| `int32` / `uint32` | 32 位整数 |
| `int64` / `uint64` | 64 位整数 |

### 3. 对齐要求

| 操作类型 | 对齐要求 |
|---------|---------|
| 大多数矢量操作 | 32 字节 |
| DataCopy | **512 字节** |
| 某些特殊 API | 64/128 字节 |

**查阅方法**：查看对应 API 文档的 **Restriction** 章节

---

## 服务器类型对照

| 服务器 | NPU 架构 | 适用场景 |
|-------|---------|---------|
| A2 | NPU ARCH 200x/220x | 较早一代 |
| A3 | NPU ARCH 351X | 新一代，性能更强 |

**当前项目**：A3 服务器

---

## 使用建议

1. **查阅 API 文档时**：
   - 优先查看 **Restriction** 章节
   - 确认 API 支持的 NPU 架构
   - 验证对齐要求

2. **使用新 API 前**：
   - 检查 CANN 版本要求
   - 在当前环境测试验证
   - 备选兼容方案

3. **开发新算子时**：
   - 确认目标服务器类型
   - 选择兼容的 API
   - 注意数据类型限制

4. **遇到兼容性问题时**：
   - 查阅 `examples/10_compatibility_cases/` 示例
   - 参考 asc-devkit 的兼容性处理方法
   - 降级使用旧版本 API

---

## 相关资源

- [API 文档索引](api-index.md)
- [示例代码目录](example-catalog.md)
