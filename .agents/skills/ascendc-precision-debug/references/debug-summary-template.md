# 精度调试总结模板

> 本模板用于精度调试过程中每一步成功后的总结记录。每次解决一个精度问题、排查一个常见陷阱或完成一个调试阶段后，应填写此模板并保存至指定位置。

---

## 本次调试信息

- **算子名称**：[例如：Sinh、Softmax、ReduceSum]
- **调试日期**：[YYYY-MM-DD]
- **问题症状**：[简要描述问题表现，如：FP16 精度误差超过 1e-3、特定输入范围出现 NaN、累加结果逐渐偏离等]

---

## 1. 文档描述不清晰的地方

记录在调试过程中发现的文档不清晰或有歧义的地方。

- [ ] **具体描述**：[哪个文档/哪部分不够清晰]
  - 位置：[文档路径或章节，如 ReduceMax API 文档（find "$ASC_DEVKIT_DIR/docs/api/" -name "ReduceMax*.md"）]
  - 问题：[描述问题，如参数说明模糊、约束条件未明确、示例代码缺失]

- [ ] **具体描述**：[补充其他不清晰的地方]
  - 位置：[文档路径]
  - 问题：[问题描述]

### 改进建议

- 对 [文档位置] 的改进建议：[如何改进描述，如补充参数范围说明、增加边界条件示例、添加注意事项]
- 对 [文档位置] 的改进建议：[改进建议]

---

## 2. 需要改进的地方及推荐方案

### API 文档

- [ ] **缺少/不清楚的 API**：[API 名称]
  - 问题：[描述问题，如缺少某些参数的说明、未说明数据类型支持、返回值说明不完整]
  - 建议如何补充：[具体建议，如补充参数取值范围表格、添加支持的 dtype 列表、增加示例代码]

### 示例代码

- [ ] **缺少哪类示例**：[示例类型]
  - 问题：[描述缺少的示例场景，如缺少非对齐输入的处理示例、缺少多核并行的完整示例]
  - 建议增加什么：[具体建议，如增加 DataCopyPad 使用示例、提供完整的双缓冲实现代码]

### 调试工具

- [ ] **工具不够好用**：[工具名称/类型]
  - 问题：[描述问题，如 Printf 输出格式不便、缺乏误差可视化工具]
  - 建议改进：[如何改进，如增加结构化输出、提供误差分布绘图脚本]

### Skill 改进 ⭐

记录对 Skill 本身（如 ascendc-precision-debug、ascendc-task-focus 等）的改进建议。

- [ ] **Skill 名称**：[例如：ascendc-precision-debug、ascendc-task-focus]
  - 问题：[描述问题，如 SKILL.md 中某部分说明不清楚、缺少某个场景的指导、调试流程不够完善]
  - 建议改进：[具体建议，如补充某个常见陷阱的说明、增加决策树、调整内容组织结构]
  - 影响程度：[高/中/低，这个问题有多大影响]

- [ ] **Skill 名称**：[补充其他 Skill 的改进建议]
  - 问题：[问题描述]
  - 建议改进：[改进建议]
  - 影响程度：[高/中/低]

### 其他

- [ ] **其他改进建议**：[类别]
  - 问题：[问题描述]
  - 建议改进：[改进建议]

---

## 3. 给 Agent 造成困扰的点

记录在调试过程中让 Agent 困惑、犹豫或浪费时间的点。

- [ ] **具体困扰**：[哪一步让 Agent 困惑]
  - 困扰描述：[详细描述，如在某个 API 的参数选择上不确定、对某个约束条件的理解有歧义、在多个实现方案之间犹豫]
  - 耗时估计：[大约浪费了多少时间或 token]

- [ ] **具体困扰**：[补充其他困扰点]
  - 困扰描述：[详细描述]
  - 耗时估计：[耗时]

### 改进建议

- 对 [困扰点] 的改进建议：[如何避免这种困扰，如增加常见错误预警、提供决策树、在 SKILL.md 中突出显示关键信息]

### 文档改进

- 需要在文档中补充的说明：[具体说明内容，如在 SKILL.md 中增加"常见误用"章节、在 API 文档中增加警告标注]

---

## 4. 调试过程记录

记录本次调试的完整过程，便于后续回顾和改进调试方法。

### 调试尝试

- **尝试次数**：[X 次]
- **使用方法**：
  - [ ] 误差分布分析
  - [ ] Printf 特定位置
  - [ ] 常见陷阱排查
  - [ ] 二分调试
  - [ ] 其他：[说明]

### 尝试过程（简述）

1. 第 1 次尝试：[方法 + 结果，如 Printf 检查 exp 计算，发现 exp(x) 正常]
2. 第 2 次尝试：[方法 + 结果]
3. ...

### 最终解决方案

- **解决方案**：[解决问题的具体方法，如将中间累加器从 FP16 改为 FP32]
- **代码位置**：[文件路径:行号，如 `ops/sinh/sinh.asc:87`]
- **关键改动**：[简述代码改动，如 `float sum_fp32 = 0.0f;` 替代 `half sum = 0.0h;`]

### 验证结果

- **测试用例**：[使用的测试规模，如 shape=(16, 16), dtype=fp16]
- **精度表现**：
  - FP32：max_error=[X], rtol=[X], atol=[X]
  - FP16：max_error=[X], rtol=[X], atol=[X]
- **通过率**：[X/X 测试通过]
- **是否达标**：[是/否，未达标说明剩余问题]

---

## 5. 经验总结（可选）

### 本次学到的经验

- [ ] [经验1：关于某个 API 的使用技巧]
- [ ] [经验2：关于某种精度问题的解决思路]
- [ ] [经验3：关于调试方法的改进]

### 可复用的调试模式

- [ ] [可复用的调试流程/代码片段/测试方法]

---

## 保存位置

根据调试类型，将总结保存到以下位置：

- **单算子调试**：`docs/{算子名}/precision-debug-summary.md`
  - 例如：`docs/sinh/precision-debug-summary.md`
- **全局调试**：`docs/precision-debug-summary.md`
  - 用于跨算子的通用精度问题总结

---

## 示例：完整填写案例

```markdown
# 精度调试总结

## 本次调试信息
- **算子名称**：Sinh
- **调试日期**：2025-01-15
- **问题症状**：FP16 精度下，输入 x > 5 时结果误差超过 1e-2

## 1. 文档描述不清晰的地方
- **具体描述**：Exp API 的返回范围说明不明确
  - 位置：Exp API 文档（find "$ASC_DEVKIT_DIR/docs/api/" -name "Exp*.md"）
  - 问题：未说明 FP16 下 Exp 的溢出阈值
  - 改进建议：补充不同 dtype 下的有效输入范围表格

## 2. 需要改进的地方

### Skill 改进
- **ascendc-precision-debug**：快速调试方法的触发条件不够明确
  - 问题：SKILL.md 中没有明确说明何时使用哪种快速方法，导致 Agent 选择困难
  - 建议改进：增加"调试方法选择决策树"，根据误差类型快速选择方法
  - 影响程度：中

### 其他
- **示例代码**：缺少处理数值溢出的完整示例
  - 建议增加：展示如何使用 max-log-sum-exp 技巧避免溢出

## 3. 给 Agent 造成困扰的点
- **具体困扰**：不知道如何判断 Exp 是否溢出
  - 困扰描述：花了 3 次尝试才发现是 exp(x) 直接溢出导致 NaN
  - 改进建议：在 SKILL.md 的"常见陷阱"中增加"exp/log 溢出"条目

## 4. 调试过程记录
- **尝试次数**：4 次
- **使用方法**：Printf 调试 + 常见陷阱排查
- **最终方案**：先减最大值再计算 exp，避免溢出
- **验证结果**：FP16 max_error 从 1e-2 降至 5e-4，通过所有测试
```
