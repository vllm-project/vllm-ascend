# Git 规范：vLLM / vllm-ascend 社区分支命名与贡献工作流

> 整理日期：2026-09-05
> 依据：vLLM 官方 contributing 指南（docs.vllm.ai）+ vllm-ascend 仓库 CONTRIBUTING.md、AGENTS.md、docs/source/developer_guide/contribution/index.md

## 一、分支命名规范

**核心原则：社区只约束官方仓的分支；个人开发分支留在自己的 fork 里，命名自由。**

### 官方仓（vllm-project）的分支结构

| 分支 | 用途 |
|------|------|
| `main` | 主干。vllm-ascend 的 main 对应 vLLM 的 main，靠昇腾 CI 持续看护 |
| `releases/vX.Y.Z` | 版本开发分支（如 `releases/v0.23.0`），只允许 bug 修复 |
| `rfc/<feature-name>` | vllm-ascend 特有的特性协作分支，唯一有文档记载的命名模式 |

### 个人分支命名惯例

PR 合入后个人分支即删除，社区不规定命名。惯例是「类型/主题」式，与 commit type 对应：

- `feat/xxx` — 新功能
- `perf/xxx` — 性能优化（算子优化工作常用）
- `fix/xxx` — bug 修复
- 或 `你的GitHub用户名/主题`，如 `huamus/optimize-rmsnorm`

## 二、往社区合代码的完整工作流

```
upstream 官方仓 ──(fork)──> 你的 GitHub fork ──(clone)──> 本地
   vllm-project/vllm-ascend     huamus/vllm-ascend        本地仓库
```

### 第 1 步：配置双 remote

```bash
git remote add upstream https://github.com/vllm-project/vllm-ascend.git
```

### 第 2 步：同步官方代码并拉分支

```bash
git fetch upstream
git checkout main
git merge --ff-only upstream/main    # 让本地 main 与官方对齐
git checkout -b perf/optimize-rmsnorm  # 从 main 拉个人分支
```

### 第 3 步：开发 + 提交

两个社区都强制 **DCO 签名**（commit 里带 `Signed-off-by:`，法律上声明你有权提交这段代码）：

```bash
git commit -s -m "perf: optimize rmsnorm kernel for NPU" -m "详细说明改了什么、为什么"
```

vllm-ascend 使用 **Conventional Commits** 格式：

```
<type>: <summary>

<body - 说明改了什么、为什么>

Signed-off-by: Your Name <your.email@example.com>
```

合法 type：`feat` `fix` `perf` `refactor` `test` `docs` `chore`

好例子与坏例子：

```txt
# 好
perf: avoid CPU-NPU sync in attention computation
fix(model_runner): correct padding token handling

# 坏
fix bug
add feature
update code
```

### 第 4 步：本地自检（vllm-ascend 要求）

```bash
bash format.sh ci      # 全仓 lint，包括 markdown
ruff check vllm_ascend/
pytest tests/ut/       # 新功能必须带单测（tests/ut/），端到端放 tests/e2e/
```

代码风格要点（来自 AGENTS.md）：

- 环境变量统一收口在 `vllm_ascend/envs.py`，命名必须 `VLLM_ASCEND_*` 前缀
- 不加新的可变全局变量；不用魔法数字
- 热路径（hot path）里避免 `tensor.item()` —— 它会触发 NPU→CPU 同步，严重拖慢性能，review 时重点盯
- 避免热路径里的 CPU-NPU 内存搬运；安全场景优先原地操作（`x.add_()` 等）
- 模型相关改动优先走 patch（`vllm_ascend/patch/`）或继承，不直接加模型文件

### 第 5 步：推送 fork 并发 PR

```bash
git push -u origin perf/optimize-rmsnorm
```

在 GitHub 上从 `huamus/vllm-ascend` 的分支向 `vllm-project/vllm-ascend` 的 main 发 PR。

### 第 6 步：PR 标题规范（方括号标签）

**vLLM**：`[Perf][Kernel] Optimize fused MoE`

- 类型标签：`[Bugfix]` `[Feature]` `[Perf]` `[Refactor]` `[CI]`/`[CI/Build]` `[Test]` `[Doc]` `[Misc]`
- 范围标签：`[Model]` `[Frontend]` `[Core]` `[Kernel]` `[Attention]` `[Quantization]` `[MoE]` `[Spec Decode]` `[LoRA]`，硬件类 `[ROCm]` `[XPU]` `[CPU]` `[Hardware][Vendor]`
- 标签可叠加：`[Bugfix][KV Offload] Fix block accounting`

**vllm-ascend**：`[Feat][Kernel] xxx`

- 类型：`[BugFix]` `[Feat]` `[Perf]` `[CI]` `[Doc]` `[Test]` `[Refactor]` `[Platform]` `[Misc]`
- 范围：`[Kernel]` `[ModelRunner]` `[Platform]` `[Worker]` `[Attention]` `[Communicator]` `[Core]` `[Model]`

做算子优化，标题大概率是 `[Perf][Kernel] ...` 或 `[Feat][Kernel] ...`。

PR 描述需按模板填写三部分：

1. What this PR does / why we need it?（做了什么、为什么需要）
2. Does this PR introduce any user-facing change?（是否引入用户可见变化）
3. How was this patch tested?（单测文件列表 / 手动测试步骤 / 仅 CI 验证）

### 其他要点

- 超过 500 行（不含 kernel/data/config/test）的大改动，必须先发 **RFC issue** 讨论设计，否则被打上 `rfc-required` 标签
- vLLM 要求 AI 辅助生成的代码必须在 PR 中声明，并加 `Co-authored-by:` 标记到 commit message
- vllm-ascend 的 CI 需要真机 NPU，提交后昇腾 CI 跑测试
- 自定义算子需按 PyTorch 规范注册（schema + 实现 + meta 函数），用 `torch.library.opcheck()` 验证注册
- vLLM 有 PR 数量限制：无写权限的贡献者最多同时 6 个 open PR；加急评审可邮件 pr-review-request@vllm.ai
- Review 节奏：PR 提交后分配 reviewer，2-3 天更新状态；7 天没动静可以主动催

## 三、本地仓库现状备忘

- 本地 `vllm-ascend` 的 origin 指向 `huamus/vllm-ascend`（个人 fork），**尚未配置 upstream remote**
- 判断 fork 的方法：`git remote -v` 看所有者段（`github.com/<所有者>/<仓库名>.git`），fork 特征是同名仓库挂在个人账号下
